<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Danh sách đăng ký sự kiện</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-5">

  <h2 class="mb-4">📋 Danh sách đăng ký sự kiện</h2>

  <% if (data.length === 0) { %>
    <div class="alert alert-warning">Chưa có sinh viên nào đăng ký sự kiện.</div>
  <% } else { %>
    <table class="table table-bordered">
      <thead class="table-dark">
        <tr>
          <th>Registration ID</th>
          <th>Số điện thoại sinh viên</th>
          <th>Tên sự kiện</th>
          <th>Ngày đăng ký</th>
        </tr>
      </thead>
      <tbody>
        <% data.forEach(reg => { %>
          <tr>
            <td><%= reg._id %></td>
            <td><%= reg.studentId.phone %></td>
            <td><%= reg.eventId.name %></td>
            <td><%= new Date(reg.registrationDate).toLocaleString() %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>

    <!-- Pagination -->
    <nav aria-label="Page navigation example">
      <ul class="pagination">
        <% for (let i = 1; i <= totalPages; i++) { %>
          <li class="page-item <%= currentPage === i ? 'active' : '' %>">
            <a class="page-link" href="?page=<%= i %>"><%= i %></a>
          </li>
        <% } %>
      </ul>
    </nav>
  <% } %>

  <a href="/admin/dashboard" class="btn btn-secondary mt-3">← Quay về Dashboard</a>

</body>
</html>
